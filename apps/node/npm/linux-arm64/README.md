# @shotkit/linux-arm64

ShotKit runtime binaries for Linux arm64 (`shotcli` + `libshot`). This package is installed automatically as an optional dependency of [`@shotkit/node`](https://www.npmjs.com/package/@shotkit/node); do not depend on it directly.
